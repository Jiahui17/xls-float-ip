# Generating Floating Point Units for Dynamatic

## Usage

```
# This is way faster than building XLS from source takes forever
bash fetch_xls_release.sh

# Generate the library
bash gen_arith_ips.sh
```
